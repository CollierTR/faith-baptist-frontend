---
const { icon="/imgs/Bible-Icon-1.svg", iconDescription, width="40px", title="Baptist", text="By principle and polity, we're independent local churches, under the Headship of Christ, congregated by regenerate baptized believers."} = Astro.props;
---

  <div class="flex flex-col place-items-center justify-center bg-white p-7 gap-5 max-w-[300px] rounded-2xl border border-accent border-3">
    <img src={icon} alt={iconDescription} width={width}/>
    <h2 class="text-2xl font-serif font-extralight">{title}</h2>
    <p class="text-center text-lg font-serif font-extralight">{text}</p>
  </div>
