---
const { link="#", openNewTab=false, text="Click Me"} = Astro.props;
let target = "_self"

if (openNewTab) {
  target = "_blank"
}
---

<a href={link} target={target}>
  <button class="text-white bg-primary-dark px-6 py-3 rounded-2xl">
    {text}
  </button>
</a>
