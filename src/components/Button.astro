---
const { link="#", openNewTab=false, text="Click Me"} = Astro.props;
let target = "_self"

if (openNewTab) {
  target = "_blank"
}
---

<a href={link} target={target}>
  <button class="text-white bg-primary px-6 py-2 rounded-lg">
    {text}
  </button>
</a>
