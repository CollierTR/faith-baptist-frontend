---
const { topText="About Us", bottomText="Our Core Principles"} = Astro.props;
---

<div class="flex justify-center py-12">
  <div class="flex flex-col place-items-center justify-center gap-3">
    <h2 class="text-2xl text-accent font-semibold">{topText}</h2>
    <p class="text-4xl font-serif font-extralight">{bottomText}</p>
  </div>
</div>
